# Implementation Log

All times use the local workspace timezone unless otherwise stated.

## 2026-05-13

- Initialized execution goal: implement M1-M7 with live logs and traceable artifacts.
- Current baseline commit before this goal: `aa84901 Establish a traceable retargeting baseline scaffold`.
- Scope decision: implement runnable M1-M4 artifacts first; M5-M7 require torch/Isaac/hardware and will receive executable scaffolds plus explicit blockers until environment is available.
- Verification rule: every milestone claim must map to files, commands, or documented blockers.
- Milestone revision: paper survey is now M1, and data split work is gated behind motion curation/quality flags because source motion and retargeted humanoid targets may be noisy or physically invalid.
- Curation research update: added NMR/CEPR, PHUMA, ExBody2, and KungfuBot-style dynamic-motion processing as filtering references; current direction is source-motion curation + humanoid-target curation + physics-refinement labels, with quality scores instead of only binary keep/drop.
- M2 implementation update: added standard-library split/curation/index code in `src/online_retarget/data/curation.py`, CLI command `split-index`, and tests in `tests/test_bones_seed_index.py`.
- Verification: `PYTHONPATH=src python3 -m unittest discover -s tests` passed 10 tests.
- Real data smoke: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py split-index --data-root /home/user/data/motion_data --output-root runs --seed 17 --train-ratio 0.8 --val-ratio 0.1 --policy-name metadata_balanced_v0 --min-duration-frames 60` wrote `runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0`.
- Real data result: 142,220 rows, 522 actors, actor split train/val/test = 417/52/53, row split train/val/test = 112,789/15,760/13,671, curation actions keep/downweight = 71,132/71,088, mirror flag count = 71,088.
- Added G1 target quality scanner in `src/online_retarget/data/g1_quality.py` plus CLI command `scan-g1-quality` and tests in `tests/test_g1_quality.py`.
- Real G1 quality smoke: scanned first 100 indexed G1 CSV targets from read-only `g1.tar` into `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit100`.
- G1 quality smoke result with temporary thresholds (`fps=30`, max joint velocity 20 rad/s, max root speed 8 m/s): 29 keep, 71 quarantine, flags `joint_velocity_jump=71`, `root_discontinuity=28`; metric summary recorded in `g1_quality_report.json`.
- Current M2 limitation: thresholds are not calibrated yet and only G1 target CSV stats are scanned. Source skeleton stats, foot slide/float, penetration, and self-intersection still need scanners before M2 is fully closed.
- M3 implementation update: added dataset schema and observation/output contract dataclasses in `src/online_retarget/data/schema.py`, including `MotionPairRef`, `ObservationSpec`, `RobotStateSpec`, and `OutputSpec`.
- Split index update: metadata index now includes morphology columns needed by `MotionPairRef`; regenerated `runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0`.
- Verification: `PYTHONPATH=src python3 -m unittest discover -s tests` passed 14 tests after M3 schema additions.
- M4 implementation update: added `src/online_retarget/evaluation.py` and CLI command `offline-eval` for JSONL prediction/target evaluation, with summary JSON, per-sample CSV, failure manifest, and actor/category/package/quality-flag aggregation.
- Verification: `PYTHONPATH=src python3 -m unittest discover -s tests` passed 15 tests after M4 eval additions; CLI help smoke passed for `split-index`, `scan-g1-quality`, and `offline-eval`.
- M5 scaffold update: `scripts/train.py --dry-run` now reads config/index/schema, reports git SHA/dirty, DDP rank/world size, observation/output dims, and sample refs without starting optimization.
- M5 dry-run evidence: `PYTHONPATH=src python3 scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` reported observation dim 1547, output dim 29, train refs 112,789.
- M6 scaffold update: added `scripts/benchmark_latency.py`; dry-run writes benchmark contract to `runs/benchmarks/baseline_mlp_dry_run.json`.
- M7 scaffold update: added `scripts/eval_isaac.py`; dry-run writes `runs/isaac_eval/dry_run/isaac_eval_status.json` with expected simulator metrics.
- Current M5-M7 blockers: tensor window extraction and optimizer loop are not implemented; real latency requires torch/CUDA/4090; real simulator eval requires Isaac Lab and G1 replay/tracking task binding.
- Status audit added at `docs/status/m1_m7_status.md`; M1-M7 are not complete yet, but current artifacts and blockers are explicitly mapped.

## 2026-05-14

- Planning update: added M2Q as an explicit motion-quality curation gate in `docs/milestones.md`. This gate covers source human motion, G1 target motion, pair consistency, physics provenance, calibrated thresholds, diversity-loss reporting, and worst-clip manifests.
- Research update: added `docs/research/motion_quality_curation.md`, synthesizing NMR/CEPR, PHUMA/PhySINK, GMR, OmniTrack, OmniRetarget, KDMR, contact/dynamics, self-contact retargeting, and foot-contact detection references into implementation-ready quality signals.
- Paper tracking update: expanded `docs/research/paper_matrix.md`, `docs/research/literature_review.md`, and `docs/research/pdf_manifest.md` so motion filtering is traceable to papers rather than only local intuition.
- M2 source curation update: added `src/online_retarget/data/bvh_quality.py`, CLI command `scan-source-quality`, and `tests/test_bvh_quality.py`.
- Real source quality smoke: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py scan-source-quality --data-root /home/user/data/motion_data --index-csv runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0/split_index.csv --output-root runs --limit 100 --max-channel-velocity 3000 --max-root-speed 500` wrote `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_source_limit100`.
- Source quality result: 100 scanned, 59 keep, 40 quarantine, 1 exclude; flags `source_channel_jump=40`, `nonfinite_value=1`, `channel_width_mismatch=1`.
- Current M2 limitation after source scanner: contact, foot slide/float, penetration, self-intersection, and calibrated threshold policy are still pending.
- M2 threshold calibration update: added `src/online_retarget/data/thresholds.py`, CLI command `propose-thresholds`, and `tests/test_thresholds.py`.
- Threshold proposal smoke: generated p95 proposals for G1 stats at `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit100/g1_threshold_proposals_p95.json` and source stats at `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_source_limit100/source_threshold_proposals_p95.json`.
- Verification: `PYTHONPATH=src python3 -m unittest discover -s tests` passed 19 tests after threshold proposal additions.
- M5 data-path update: added `src/online_retarget/data/supervised_builder.py`, CLI command `build-supervised-jsonl`, and `tests/test_supervised_builder.py`.
- Real supervised debug artifact: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py build-supervised-jsonl --data-root /home/user/data/motion_data --index-csv runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0/split_index.csv --output-root runs --split train --limit 8 --history-frames 8` wrote `runs/supervised/train_h8_limit8`.
- Supervised debug result: 8 samples, 0 skipped, raw-BVH-channel input dim 1933, G1 output dim 29. This is a data-path debug builder, not the final 30-body observation contract.
- M2 merge update: added `src/online_retarget/data/quality_merge.py`, CLI command `merge-quality`, and `tests/test_quality_merge.py`.
- Curated index smoke: merged split index with first-100 source and G1 quality stats into `runs/curated/smoke_source_g1_limit100/curated_index.csv`.
- Curated index result: 142,220 rows; merged source rows 100, merged G1 rows 100; actions keep/downweight/quarantine/exclude = 71,095/71,051/73/1.
- M5 builder update: `build-supervised-jsonl` now supports `--action-column`, allowing debug sample generation from `merged_quality_action` in curated indexes.
- Curated supervised debug artifact: generated `runs/supervised/train_merged-quality-action_h8_limit8` from `runs/curated/smoke_source_g1_limit100/curated_index.csv` with merged actions keep/downweight only.
- M5 training update: `scripts/train.py` now has a real PyTorch optimizer loop for supervised JSONL artifacts (`--samples-jsonl`) and saves `checkpoint.pt` plus `train_report.json` when torch is available.
- M5 dry-run evidence: `PYTHONPATH=src:. python3 scripts/train.py --config configs/baseline_mlp.yaml --samples-jsonl runs/supervised/train_merged-quality-action_h8_limit8/samples.jsonl --dry-run --limit 1` succeeds and reports the samples path.
- M5 environment blocker evidence: non-dry-run exits with `Training requires the conda environment from environment.yml with torch installed.` in the current base Python, which lacks torch/numpy.
- M5/M2Q gate update: `scripts/train.py` now collects a `quality_gate` context and refuses formal non-dry-run training unless a quality policy ID, existing quality report, curated index path, and `merged_quality_action` are present. `--allow-debug-data` exists only for explicitly labeled debug runs.
- Config update: `configs/baseline_mlp.yaml` now points to the smoke curated index, merged-action supervised JSONL, quality policy ID `smoke_source_g1_limit100`, and curated report path.
- M2Q artifact update: `merge-quality` now writes `worst_clips.csv` and a curated-report breakdown by split, package, and category. Regenerated `runs/curated/smoke_source_g1_limit100`, producing 74 worst-clip rows plus the 142,220-row curated index.
- M3/M5 dataset update: added `src/online_retarget/data/windowed_builder.py`, CLI command `build-windowed-jsonl`, and `tests/test_windowed_builder.py`. This generates schema-compatible BVH-FK 30-body position/velocity windows with zero-filled robot-state placeholders.
- Real 30-body smoke artifact: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py build-windowed-jsonl --data-root /home/user/data/motion_data --index-csv runs/curated/smoke_source_g1_limit100/curated_index.csv --output-root runs --split train --curation-action keep --curation-action downweight --action-column merged_quality_action --limit 4 --history-frames 8` wrote `runs/supervised/train_merged-quality-action_30b_h8_limit4`, with 4 samples, input dim 1547, output dim 29, and 0 skipped.
- 2026-05-14 02:29 CST M5 gate update: `scripts/train.py` now records sample-manifest builder metadata in `quality_gate` and refuses formal training unless supervised samples were built by `bvh_fk_30body_window`. Raw `raw_bvh_channel_debug` artifacts remain allowed only with `--allow-debug-data`.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 33 tests OK; `PYTHONPATH=src python3 -m py_compile scripts/train.py src/online_retarget/data/windowed_builder.py src/online_retarget/data/__init__.py src/online_retarget/cli.py` passed; dry-run training reported `samples_builder_is_formal=true`; non-dry-run raw debug sample check failed as intended with "formal samples built by bvh_fk_30body_window".
- M2Q FK/contact update: added `src/online_retarget/data/source_fk_quality.py`, CLI command `scan-source-fk-quality`, and `tests/test_source_fk_quality.py`. The scanner uses BVH FK foot/body positions to report contact ratio, foot slide proxy, mean/max foot clearance, penetration depth, and nonfinite FK positions; `--frame-stride` and `--max-frames` keep smoke scans bounded.
- Real source FK/contact smoke: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py scan-source-fk-quality --data-root /home/user/data/motion_data --index-csv runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0/split_index.csv --output-root runs --limit 100 --ground-height 0.0 --fps 30 --frame-stride 2 --max-frames 256 --contact-height-threshold 0.04 --max-contact-slide-speed 0.25 --max-mean-foot-clearance 0.10 --max-penetration-depth 0.03 --min-contact-frame-ratio 0.05` wrote `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_source_fk_limit100`, with keep/downweight/quarantine = 42/20/38 and flags `source_foot_slide=20`, `source_low_foot_contact=38`.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 39 tests OK; targeted `py_compile` for the train/CLI/source-FK/windowed modules passed; `scripts/train.py --dry-run` still reports `samples_builder_is_formal=true`.
- M5 traceability update: `scripts/train.py` now writes `train_predictions.jsonl`, runs M4 `evaluate_jsonl` by default after a successful torch training loop, records the offline eval artifact in `train_report.json`, and supports optional WandB metadata/artifact logging through `tracking.wandb_mode`. The default config keeps WandB disabled so local runs do not require login/network.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 42 tests OK; targeted `py_compile` passed; `scripts/train.py --dry-run` still reports `samples_builder_is_formal=true`. Real execution of the post-train eval path remains blocked until torch is available.
- M2Q G1 FK/contact update: extended `src/online_retarget/data/g1_quality.py` and CLI `scan-g1-quality` with optional MJCF FK, centimeter-to-meter and degree-to-radian scaling, G1 joint-limit checks, foot contact ratio, foot slide, foot float, ground penetration, and start/end root stability metrics. The scanner remains standard-library only and does not require MuJoCo at scan time.
- Tests: `tests/test_g1_quality.py` now covers CSV-only compatibility plus MJCF FK contact/penetration/joint-limit flags. `tests/test_quality_merge.py` now checks that G1 FK/contact metrics are included in `worst_clips.csv`.
- Real G1 MJCF FK/contact smoke: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py scan-g1-quality --data-root /home/user/data/motion_data --index-csv runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0/split_index.csv --output-root runs --limit 100 --fps 30 --max-joint-velocity 20 --max-root-speed 8 --model-xml /home/user/repos/GMR/assets/unitree_g1/g1_mocap_29dof.xml --root-position-scale 0.01 --joint-angle-scale 0.017453292519943295 --root-rotation-scale 0.017453292519943295 --frame-stride 2 --max-frames 256 --ground-height 0.0 --contact-height-threshold 0.04 --max-contact-slide-speed 0.25 --max-mean-foot-clearance 0.10 --max-penetration-depth 0.03 --min-contact-frame-ratio 0.05` refreshed `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit100/g1_quality_report.json`.
- G1 MJCF FK/contact smoke result: scanned 100 clips with model `/home/user/repos/GMR/assets/unitree_g1/g1_mocap_29dof.xml`; actions keep/downweight/quarantine = 19/36/45; flags `g1_foot_slide=70`, `g1_ground_penetration=41`, `g1_joint_limit_violation=18`, `g1_unstable_start_end=8`, `g1_foot_float=1`. This is still a smoke calibration signal, not a final full-dataset curation policy.
- Curated smoke refresh: reran `merge-quality` with the updated G1 stats, producing `runs/curated/smoke_source_g1_limit100` with actions keep/downweight/quarantine/exclude = 71,091/71,069/59/1 and G1 FK/contact metrics in the worst-clip manifest.
- Planning update: refined M2Q after user emphasized that motion itself may be imperfect. `docs/milestones.md`, `docs/research/motion_quality_curation.md`, `docs/research/paper_matrix.md`, and `docs/research/literature_review.md` now state that filtering is a first-class milestone, not a hidden split/train detail.
- Literature update: M2Q now explicitly uses NMR/CEPR, PHUMA/PhySINK, GMR, OmniTrack, OmniRetarget, KDMR, ExBody2, and contact-metric work to justify source/G1/pair/physics quality labels. The current design answer is staged `keep/downweight/quarantine/exclude`, with category-aware thresholds and diversity-loss review before formal training.
- M2Q merge update: extended `merge-quality` with `--source-fk-stats-jsonl` so source BVH discontinuity stats, source FK/contact stats, and G1 FK/contact stats are merged independently. Curated rows now include `source_fk_quality_action` and `source_fk_quality_flags`; `worst_clips.csv` includes source FK contact ratio, slide, clearance, and penetration metrics.
- Curated smoke refresh: reran three-way `merge-quality` over first-100 source BVH, source FK/contact, and G1 FK/contact stats. `runs/curated/smoke_source_g1_limit100/curated_report.json` now records keep/downweight/quarantine/exclude = 71,088/71,048/83/1, `merged_source_rows=100`, `merged_source_fk_rows=100`, `merged_g1_rows=100`, and flags `source_fk:source_foot_slide=20`, `source_fk:source_low_foot_contact=38`.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 44 tests OK; targeted `py_compile` for train/inspect/quality-merge/CLI/test files passed; `scripts/train.py --dry-run --limit 1` still reports `samples_builder_is_formal=true`; rebuilding the 30-body window smoke artifact succeeded with 4 samples and 0 skipped.
- M2Q diversity-loss update: `merge-quality` now adds `diversity_loss` to `curated_report.json` for actor, source skeleton, actor height bin, gender, package, category, split, and mirror status. The current smoke policy keeps all 522 actor/source-skeleton groups represented while quarantine/exclude affects 84 rows.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 44 tests OK; targeted `py_compile` for `quality_merge.py`, CLI, and quality-merge tests passed; the three-way `merge-quality` smoke report regenerated successfully with diversity-loss summaries.
- M2Q grouped-threshold update: `propose-thresholds` now supports repeated `--group-by` fields plus `--min-group-size`, so threshold proposals can be reported by category/package/split instead of only globally. Source BVH, source FK/contact, and G1 scanners now carry package/category/mirror/gender metadata into stats JSONL where available.
- Grouped threshold smoke artifacts: generated `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_source_fk_limit100/source_fk_threshold_proposals_grouped_p95.json` from the 100-clip source FK/contact smoke stats grouped by category/package/split, and generated `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit6/g1_threshold_proposals_grouped_p95.json` after a 6-clip G1 MJCF FK/contact metadata verification scan.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 45 tests OK; `scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` still reports `samples_builder_is_formal=true`; targeted `py_compile` for threshold/scanner/CLI/test files passed. Grouped smoke thresholds are still proposals, not a promoted formal curation policy.
- M2Q representative-scan update: added `src/online_retarget/data/row_sampling.py` and `--sample-by` to `scan-source-quality`, `scan-source-fk-quality`, and `scan-g1-quality`. Default scans keep the previous first-N behavior; explicit `--sample-by category --sample-by split` uses deterministic round-robin sampling and writes candidate/selected group counts into each quality report.
- Stratified source FK/contact smoke: `PYTHONPATH=src python3 scripts/inspect_bones_seed.py scan-source-fk-quality --data-root /home/user/data/motion_data --index-csv runs/indices/actor_split_t80_v10_x10_s17_metadata_balanced_v0/split_index.csv --output-root runs --limit 24 --sample-by category --sample-by split --ground-height 0.0 --fps 30 --frame-stride 4 --max-frames 128 --contact-height-threshold 0.04 --max-contact-slide-speed 0.25 --max-mean-foot-clearance 0.10 --max-penetration-depth 0.03 --min-contact-frame-ratio 0.05` wrote `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_source_fk_limit24_by-category-split`, scanned 24 rows across 24 category/split groups, and reported keep/downweight/quarantine = 18/3/3.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 52 tests OK; `scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` still reports `samples_builder_is_formal=true`; targeted `py_compile` for sampling/scanner/CLI/test files passed.
- M2Q manual-review update: added `src/online_retarget/data/review_manifest.py` and CLI `build-review-manifest`, producing JSONL and Markdown review sheets from `worst_clips.csv` grouped by failure family. The review items include source BVH path, G1 CSV path, actor/category/split, merged flags, key metrics, and blank reviewer decision fields.
- Worst-clip path fix: `merge-quality` now carries `move_soma_proportional_path` and `move_g1_path` into `worst_clips.csv`, so manual review manifests can point directly to the original read-only source and target files.
- Manual review smoke: regenerated `runs/curated/smoke_source_g1_limit100` and then ran `PYTHONPATH=src python3 scripts/inspect_bones_seed.py build-review-manifest --worst-clips-csv runs/curated/smoke_source_g1_limit100/worst_clips.csv --output-root runs/curated/smoke_source_g1_limit100 --run-name manual_review --max-per-family 5`, producing 31 review items across parser, mirror, jump, foot-slide, penetration, float, and joint-limit families under `runs/curated/smoke_source_g1_limit100/manual_review`.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 53 tests OK; `scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` still reports `samples_builder_is_formal=true`; targeted `py_compile` for review/merge/CLI/test files passed.
- M2Q G1 self-collision proxy update: extended `src/online_retarget/data/g1_quality.py` and CLI `scan-g1-quality` with a lightweight MJCF FK geom-point distance proxy for non-adjacent body pairs. The proxy ignores auxiliary mocap/IMU bodies and uses `min_self_collision_kinematic_hops=4` by default after a 12-clip stratified smoke showed `hops=3` mostly measured structural pelvis-torso proximity instead of useful self-intersection risk.
- Merge/review update: `merge-quality` now carries G1 self-collision proxy metrics into `worst_clips.csv`, and `build-review-manifest` groups `g1_self_collision_proxy` under a `self_collision` review family.
- Real G1 MJCF FK/contact/self-collision-proxy smoke: refreshed `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit100/g1_quality_report.json` over 100 clips with `/home/user/repos/GMR/assets/unitree_g1/g1_mocap_29dof.xml`, `frame_stride=2`, `max_frames=256`, `min_self_collision_distance=0.015`, and `min_self_collision_kinematic_hops=4`. Result: keep/downweight/quarantine = 18/36/46; flags `g1_foot_slide=70`, `g1_ground_penetration=41`, `g1_joint_limit_violation=18`, `g1_unstable_start_end=8`, `g1_foot_float=1`, `g1_self_collision_proxy=1`.
- Curated/review refresh: reran three-way `merge-quality` and manual-review manifest generation. `runs/curated/smoke_source_g1_limit100/curated_report.json` records keep/downweight/quarantine/exclude = 71,088/71,048/83/1 with `g1:g1_self_collision_proxy=1`; `runs/curated/smoke_source_g1_limit100/manual_review/review_report.json` now has 32 review items and includes one `self_collision` family item.
- Threshold proposal update: generated `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_limit100/g1_threshold_proposals_with_self_collision_p95.json` with `self_collision_proxy_rate` as a high-is-bad proposal metric. `min_self_collision_distance` remains a diagnostic metric, not a p95 quarantine threshold.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 54 tests OK; targeted `py_compile` for G1 quality, CLI, merge, and review modules passed. This closes the lightweight self-collision proxy implementation only; simulator/MuJoCo collision labels, full scans, calibrated policy promotion, and manual review decisions remain open.
- M2Q representative evidence update: generated category/split-stratified 560-row source BVH, source FK/contact, and G1 FK/contact/self-collision-proxy scans under `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_*_limit560_by-category-split`. Results were source BVH keep/quarantine/exclude = 266/287/7, source FK keep/downweight/quarantine/exclude = 341/119/96/4, and G1 keep/downweight/quarantine = 31/163/366.
- Representative curation merge: `runs/curated/representative_source_g1_limit560_by_category_split/curated_report.json` records keep/downweight/quarantine/exclude = 70,858/70,879/476/7 across the full split index after merging 560 source BVH rows, 560 source FK rows, and 560 G1 rows. Diversity loss remains zero lost groups for actor/source skeleton/category/split under this representative policy, but the policy is still not promoted.
- Manual review representative artifact: `runs/curated/representative_source_g1_limit560_by_category_split/manual_review/review_report.json` records 55 review rows across float, foot-slide, joint-limit, jump, mirror, parser, and penetration families. Self-collision did not enter this representative worst-clip manifest because higher-severity families dominated the top rows.
- Threshold proposal update: `propose-thresholds` now supports `--lower-metric` for low-is-bad metrics such as `contact_frame_ratio`, while `--metric` remains high-is-bad. Representative grouped p95 proposal artifacts for source FK and G1 now record `lower_metrics=["contact_frame_ratio"]` and use lower-tail p5 thresholds for contact ratio.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 57 tests OK; `PYTHONPATH=src:. python3 scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` still reports `samples_builder_is_formal=true`; `PYTHONPATH=src:. python3 -m py_compile src/online_retarget/data/thresholds.py src/online_retarget/cli.py tests/test_thresholds.py` passed; `git diff --check` passed.
- M2Q timing correction: representative source/G1 pair scans show BONES-SEED BVH `Frame Time` is `0.008333333333333333` and G1 frame counts match source frame counts. G1 quality and pair-quality defaults are now 120 Hz; source FK/contact derives FPS from the BVH header unless `--fps` is explicitly supplied. Older 30 Hz smoke velocity/contact metrics are scanner-regression evidence only.
- M2Q pair-quality update: added `src/online_retarget/data/pair_quality.py`, CLI `scan-pair-quality`, and `tests/test_pair_quality.py`. The representative command scanned 560 category/split rows with `--expected-source-frame-time 0.008333333333333333 --g1-fps 120 --max-frame-count-delta 0 --max-duration-delta-sec 0.001 --target-provenance kinematic_g1_csv`, writing `runs/quality/actor_split_t80_v10_x10_s17_metadata_balanced_v0_pair_limit560_by-category-split`. Result: keep/quarantine = 494/66, all flags `pair_duration_mismatch`, max frame-count delta 0, p95 absolute duration delta 0.001768 s.
- M2Q representative refresh: reran source FK/contact without an FPS override and G1 FK/contact/self-collision with default 120 Hz for the 560-row category/split representative sample. Source FK result is keep/downweight/quarantine/exclude = 230/230/96/4 with `source_foot_slide=244`; G1 result is keep/downweight/quarantine = 30/162/368 with `joint_velocity_jump=96`, `g1_foot_slide=485`, `g1_unstable_start_end=168`, and existing contact/joint-limit/collision flags.
- M2Q four-way merge: refreshed `runs/curated/representative_source_g1_pair_limit560_by_category_split` using source BVH, source FK, G1, and pair stats. Result across the full split index: keep/downweight/quarantine/exclude = 70,858/70,876/479/7, with 560 merged rows for each stats source and 0 lost actor/source-skeleton/category/split groups. Manual review manifest now has 35 rows across float, foot-slide, joint-limit, jump, mirror, parser, and penetration families.
- Documentation update: README, data inventory, M2Q curation notes, status audit, and completion audit now record 120 Hz timing, pair-quality scanning, and the latest four-way representative curation artifact. These representative artifacts remain calibration evidence, not a promoted formal curation policy.
- Research deep-read update: added `docs/research/papers/motion_filtering_deep_read.md` after PDF/OpenAlex review of NMR, PHUMA, GMR, KDMR, ReActor, OmniRetarget, KungfuBot, and RoboForge. The note records source/G1/pair/physics filtering signals, OpenAlex resolution status, local scratch PDF paths, and M2Q implementation priorities: acceleration/jerk, support-base, pelvis/root-height, contact-mask correction candidates, and simulator-refined provenance.
- Citation correction: OpenAlex resolved KungfuBot as arXiv `2506.12851`; `docs/research/pdf_manifest.md`, `docs/research/bibliography.bib`, and `docs/research/literature_review.md` now use that ID instead of the earlier placeholder. RoboForge `2603.17927` was added as a filtering/refinement-loop reference.
- M2Q dynamic-quality update: added source BVH and G1 acceleration/jerk metrics to `scan-source-quality` and `scan-g1-quality`. These are metric-only by default; optional CLI thresholds can emit `source_channel_acceleration_jump`, `source_root_acceleration_jump`, `source_root_jerk_jump`, `g1_joint_acceleration_jump`, `g1_root_acceleration_jump`, and `g1_root_jerk_jump` after calibration. `merge-quality` now carries dynamic metrics into `worst_clips.csv`.
- Filtering/refinement research update: added DynaRetarget and SPIDER to the paper matrix, bibliography, PDF manifest, literature review, and filtering deep-read notes. Both are treated as evidence for future simulator-refinement quality labels (`sim_refine_success` / failure / contact correctness / compute budget), not as current replacements for kinematic BONES-SEED G1 CSV targets.
- Verification: `PYTHONPATH=src:. python3 -m unittest discover -s tests` reported 67 tests OK; targeted `py_compile` for CLI/source-G1/merge quality modules and tests passed; `PYTHONPATH=src:. python3 scripts/train.py --config configs/baseline_mlp.yaml --dry-run --limit 1` still reports `samples_builder_is_formal=true`; `git diff --check` passed.
