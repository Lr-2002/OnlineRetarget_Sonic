export ONLINE_RETARGET_DATA_ROOT="/home/user/data/motion_data"
export ONLINE_RETARGET_OUTPUT_ROOT="$PWD/runs"
export WANDB_PROJECT="online-retarget"

if command -v conda >/dev/null 2>&1; then
  source "$(conda info --base)/etc/profile.d/conda.sh"
  conda activate online-retarget
fi
